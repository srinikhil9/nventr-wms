# FRP Server config — runs on EC2
# Deploy to: ~/frp/frps.ini on your EC2 instance
#
# IMPORTANT: Replace the token with a strong secret before deploying.
# Ensure EC2 Security Group allows inbound TCP on ports 7000, 6000, 6100.

[common]
bind_port = 7000
token = arvindh-srinikhil
